{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-restricted-syntax": [
      "error",
      {
        "selector": "JSXOpeningElement[name.name='AppShell'] JSXAttribute[name.name='subtitle']",
        "message": "🚫 Do not use `subtitle` on <AppShell>. Render subtitle text inside the page body instead."
      }
    ]
  }
}
